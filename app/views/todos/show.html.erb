<div class="pt-5 pl-10 flex">
 <%= link_to "戻る", todos_path %>
</div>
<div>
  <p class="pl-10 pt-10 font-bold text-xl"><%= @todo.title %></p>
  <% if @todo.category.name %>
    <p class="pl-10">カテゴリー: <%= @todo.category.name %></p>
  <% end %>
  <p class="pl-10"><%= @todo.content %></p>
</div>